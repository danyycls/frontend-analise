import AnaliseDeputados from '@/features/analise/ui/AnaliseDeputados';

export default function DeputadosPage() {
  return (
    <div className="tab-page">
      <AnaliseDeputados onFechar={() => {}} />
    </div>
  );
}

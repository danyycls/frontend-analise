import { InvestigativePanel } from '@/features/investigative-panel'

export default function InvestigativePanelPage() {
  return (
    <div className="tab-page" style={{ padding: 0, height: 'calc(100vh - 56px)' }}>
      <InvestigativePanel />
    </div>
  )
}
